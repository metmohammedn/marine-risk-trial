# Marine Risk Dashboard — Pinned Dependencies
# Generated from venv on 2026-04-10 for reproducible Docker builds

# Core Framework
dash==4.0.0
dash-mantine-components==2.6.0
dash-iconify==0.1.2
dash-leaflet==1.1.3

# Data Processing
pandas==3.0.1
numpy==2.4.2
plotly==6.6.0

# HTTP Client
httpx==0.28.1

# Caching (optional — app runs without Redis)
redis==7.2.1

# PDF Generation
reportlab==4.4.10

# Cloud data access (AODN wave buoy observations via S3, BoM API auth via Cognito)
boto3==1.42.61
pyarrow==23.0.1

# BoM Weather API — netCDF response parsing for ACCESS-G / ACCESS-GE wind forecasts.
# h5py is required by h5netcdf to parse in-memory BytesIO buffers; without it the
# BoM fetch silently fails and the ACCESS models show as unavailable.
xarray==2026.2.0
h5netcdf==1.8.1
h5py==3.16.0

# Utilities
python-dotenv==1.2.2
pytz==2023.4

# Production
gunicorn==25.3.0
